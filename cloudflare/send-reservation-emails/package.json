{
  "name": "chip-shop-bxtn-reservation-emails",
  "private": true,
  "description": "Cloudflare Worker — sends reservation confirmation/alert emails via Brevo's HTTP API. Ported from the old Netlify Function of the same name (see git history) when the site moved to GitHub Pages; tried raw SMTP (doesn't work reliably from a Worker) then SendGrid (lost its permanent free tier) before landing on Brevo.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
