{
  "name": "chip-shop-bxtn-app",
  "private": true,
  "description": "Dependencies for netlify/functions/ — the site itself is a static app with no build step.",
  "dependencies": {
    "nodemailer": "^9.0.5"
  }
}
