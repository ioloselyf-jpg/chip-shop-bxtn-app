# Build artifacts
chip-shop-bxtn-deploy.zip
*.zip

# Claude Code local settings (contains machine-specific data; may include
# sensitive command history / credentials pasted into permission allowlists)
.claude/settings.local.json

# OS junk
Thumbs.db
desktop.ini
.DS_Store

# Editor/local
*.log
/tmp/

# Local Netlify folder (retained only in case old Netlify CLI state lingers
# locally; the site no longer deploys to Netlify)
.netlify

# Node dependencies — reinstalled via npm install, not committed. Only
# cloudflare/send-reservation-emails/ has a package.json now.
node_modules
