# Build artifacts
chip-shop-bxtn-deploy.zip
*.zip

# Claude Code local settings (contains machine-specific data; may include
# sensitive command history / credentials pasted into permission allowlists)
.claude/settings.local.json

# OS junk
Thumbs.db
desktop.ini
.DS_Store

# Editor/local
*.log
/tmp/

# Local Netlify folder
.netlify

# Node dependencies for netlify/functions/ — reinstalled via npm install,
# not committed
node_modules
